extends Layout.pug

block content
  section(class="container")
    div(class="row d-flex justify-content-center")
        div(class="col-md-5 justify-content-center")
          div(class="col-sm-12 shadow-lg p-4 bg-light")
              div(class="text-center")
                  h3(class="text-dark") ลงชื่อเข้าใช้
                    form(action="/SignIn", method="POST")
                      div(class="p-4")
                          div(class="input-group mb-3")
                              span(class="input-group-text bg-dark")
                                i(class="bi bi-person-plus-fill text-white")
                              input(type="text" class="form-control" name='username' placeholder="ชื่อบัญชีผู้ใช้" maxlength=30 required)
                          div(class="input-group mb-3")
                              span(class="input-group-text bg-dark")
                                i(class="bi bi-key-fill text-white")
                              input(type="password" class="form-control" name='password' placeholder="รหัสผ่าน" maxlength=12 minlength=6 required)
                          button(class="btn btn-dark text-center mt-2" type="submit") ลงชื่อเข้าใช้
                          if checkOutNoti
                            if (signInError != '')
                                div(class="alert alert-danger py-3" role="alert")=signInError
                            else
                                div(class="alert alert-danger py-3" role="alert")=checkOutNoti
                          else
                            if (signInError != '')
                                div(class="alert alert-danger md-3" role="alert")=signInError
        div(class="col-md-5 justify-content-center")
           div(class="col-sm-12 shadow-lg p-4 bg-light")
              div(class="text-center")
                h3(class="text-dark") สมัครสมาชิก
                  div(class="p-4")    
                          div(class="input-group p-4 mb-3")
                               | ยังไม่ได้เป็นสมาชิก? 
                               | สมัครเป็นสมาชิกฟรีและเรียบง่าย!
                               | เพลิดเพลินกับการช้อปปิ้ง
                          a(class="btn btn-dark text-center mt-2" href="/SignUp" ) สมัครสมาชิกตอนนี้